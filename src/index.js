import Golem from 'src\golem.js';
let canvas = document.getElementById("gameScreen");
let ctx = canvas.getContext("2d");

const gameWidth = 800;
const gameHeight = 600;

ctx.clearRect(0, 0, 800, 600);

Golem;